import React from 'react';
import { Box, Typography } from '@mui/material';

const DeviceList = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        디바이스 목록
      </Typography>
      {/* 여기에 디바이스 목록 관련 내용 추가 */}
    </Box>
  );
};

export default DeviceList;
